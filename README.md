# Drone Flight-Time Calculator

## Project Overview
A Python-based utility to calculate a drone's expected flight time based on payload weight, complete with validation, clamping logic, and table generation.

## AI Usage & Attribution Disclosure
* Used GitHub Copilot for inline line suggestions and code completions during the implementation of `calculate_flight_time()`.
* Used GitHub Copilot Chat (`/tests`) to generate the initial unit test skeletons. 
* Personally verified and tested all code logic and confirmed all tests pass successfully using `python -m pytest`.

## Running Tests
To run the test suite, execute the following command in your terminal:
```bash
python -m pytest